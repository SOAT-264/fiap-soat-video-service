apiVersion: v1
kind: Service
metadata:
  name: video-api-service
  namespace: video-processor
spec:
  selector:
    app: video-api-service
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
